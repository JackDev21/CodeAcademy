/*El operador lógico AND && verifica dos valores y devuelve un booleano. Si ambos valores son verdaderos, devuelve true. Si uno o ambos valores son falsos, devuelve false.*/

true && true;      // true
1 > 2 && 2 > 1;    // false
true && false;     // false
4 === 4 && 3 > 1;  // true