/*Una declaración else puede agregarse a un bloque if o a una serie de bloques if-else if. El bloque else se ejecutará solo si la condición del if falla.*/


const isTaskCompleted = false;

if (isTaskCompleted) {
    console.log('Task completed');
} else {
    console.log('Task incomplete');
}