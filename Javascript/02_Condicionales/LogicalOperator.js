/*El operador lógico NOT ! se puede utilizar para hacer una de las siguientes acciones:

- Invertir un valor booleano.
- Invertir la veracidad de valores que no son booleanos.*/

let lateToWork = true;
let oppositeValue = !lateToWork;

console.log(oppositeValue);
// Prints: false