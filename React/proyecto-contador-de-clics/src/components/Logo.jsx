import React from "react";
import logo from "../img/logo.png";

const Logo = () => {
	return (
		<div className='logo-contenedor'>
			<img className='logo' src={logo} alt='logo de jack' />
		</div>
	);
};

export default Logo;
